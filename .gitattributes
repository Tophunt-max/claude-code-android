# Shell scripts must keep LF endings. A CRLF setup.sh fails on Android with
# a confusing "bad interpreter: no such file or directory" error, because the
# shebang line ends up as "...bash\r".
*.sh text eol=lf
